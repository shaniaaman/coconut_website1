<%= form_with(model: seat_class, local: true) do |form| %>
  <% if seat_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seat_class.errors.count, "error") %> prohibited this seat_class from being saved:</h2>

      <ul>
      <% seat_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= form.label "Seat Class", class: 'control-label' %>
   <div class="controls">
    	<%= form.select(:name, options_for_select([['First Class', 'First Class'], ['Business Class', 'Business Class'], ['Economy Plus', 'Economy Plus'], ['Economy', 'Economy']])) %>
    </div>
  </div>
  
  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
